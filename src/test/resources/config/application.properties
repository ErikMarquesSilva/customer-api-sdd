# Test-only additions, loaded on top of the main application.properties (config/ is a second default location).
# Hibernate statistics prove the grouping endpoint runs one statement and loads no entity (GEO-016, GEO-017).
spring.jpa.properties.hibernate.generate_statistics=true
